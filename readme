Script to bulk populate firebase storage from Json files

run 'pip install firebase_admin'


Download your firebase json credentials and place it inside cred folder,
rename the file to cred.json 

Place all your json data for upload in jsonFiles folder, the name of the json
files will be also be the collection name on the firebase database.

Run the script 

